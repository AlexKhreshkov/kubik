server:
  port: 8080

spring:
  application:
    name: consumer
  kafka:
    bootstrap-servers: kafka.producer-consumer-kafka-demo.svc.cluster.local:9092
    consumer:
      group-id: my-group
      auto-offset-reset: earliest
